package com.yahoo.dtf.actions.protocol.deploy;

import com.yahoo.dtf.actions.Action;
import com.yahoo.dtf.exception.DTFException;

/**
 * @dtf.feature Deployment Tags
 * @dtf.feature.group Deployment
 * @dtf.feature.desc 
 * <p>
 * Tag documentation for all of the deployment tags is written up in the usual
 * way that new actions are documented within DTF except they're only accessible
 * within this feature documentation instead of being visible at the normal 
 * XML tag level documentation. There are only 3 tags used within the current
 * configuation XMl and they are the following:
 * </p>
 *
 * <li>{@dtf.link dtfc}</li>
 * <li>{@dtf.link dtfa}</li>
 * <li>{@dtf.link dtfx}</li>
 * 
 */
public class Setup extends Action {

    @Override
    public void execute() throws DTFException {
        
    }
}
